services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: houseflavor
      POSTGRES_PASSWORD: houseflavor
      POSTGRES_DB: houseflavor
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
